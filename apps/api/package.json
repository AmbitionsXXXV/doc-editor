{
  "name": "doc-editor-api",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "cargo watch -x run",
    "build": "cargo build --release",
    "lint": "cargo clippy -- -D warnings",
    "format": "cargo fmt",
    "typecheck": "cargo check",
    "test": "cargo test",
    "clean": "cargo clean && node ../../tools/clean.js target node_modules .turbo"
  }
}
